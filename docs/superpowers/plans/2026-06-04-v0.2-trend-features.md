# Digital Nutrition Label v0.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 v0.1 MVP 增加"时间维度" — 历史数据存储、趋势对比、每日趋势图，让用户能看见自己的成长轨迹。

**Architecture:**
- 历史存储：每份报告的 `report_data` 序列化为 JSON 存到 `~/.digital-nutrition/history/`
- 趋势对比：读最近 N 份历史报告，对比本周与上周
- 趋势图：把每日 7 天数据聚合成堆叠柱状图，渲染到第二屏
- 趋势洞察：生成"对比上周，代码时间↑20%"类洞察

**Tech Stack:** Python 3.12, Jinja2, ECharts 5, pytest, TDD

---

## File Structure

| 文件 | 职责 | 状态 |
|------|------|------|
| `scripts/history.py` | 历史报告存储/读取 | 新建 |
| `scripts/trend.py` | 趋势对比 + 每日聚合 | 新建 |
| `scripts/insight.py` | 扩展：增加 `generate_trend_insight()` | 修改 |
| `scripts/main.py` | 调用 history/trend，传递到 report | 修改 |
| `scripts/report_generator.py` | 接收趋势数据 | 修改 |
| `templates/report.html.j2` | 增加每日趋势图区块 | 修改 |
| `tests/test_history.py` | 历史模块测试 | 新建 |
| `tests/test_trend.py` | 趋势模块测试 | 新建 |
| `tests/test_insight.py` | 增加趋势洞察测试 | 修改 |
| `tests/test_e2e.py` | 验证历史文件被写入 | 修改 |

---

## Task 1: 历史报告存储模块

**Files:**
- Create: `digital-nutrition/scripts/history.py`
- Create: `digital-nutrition/tests/test_history.py`

- [ ] **Step 1: 写失败的测试**

写入 `tests/test_history.py`:
```python
"""Tests for history storage."""
import json
from datetime import datetime
from pathlib import Path
import pytest
from scripts.history import (
    get_history_dir,
    save_report,
    load_history,
    list_reports,
)


def test_get_history_dir():
    """默认历史目录"""
    d = get_history_dir()
    assert d.name == "history"
    assert "digital-nutrition" in str(d)


def test_save_report_creates_file(tmp_path, monkeypatch):
    """保存报告创建 JSON 文件"""
    monkeypatch.setattr("scripts.history.get_history_dir", lambda: tmp_path / "history")
    report_data = {"by_category": {"code": 1000}, "total_seconds": 1000}

    path = save_report(report_data, persona="🧱 代码机器人", insights=["insight1"])

    assert path.exists()
    assert path.suffix == ".json"
    data = json.loads(path.read_text(encoding="utf-8"))
    assert data["by_category"]["code"] == 1000
    assert data["persona"] == "🧱 代码机器人"


def test_save_report_filename_includes_date(tmp_path, monkeypatch):
    """文件名包含日期"""
    monkeypatch.setattr("scripts.history.get_history_dir", lambda: tmp_path / "history")
    path = save_report({}, persona="x", insights=[])
    # 文件名格式: YYYY-MM-DD_HHMMSS.json
    assert path.stem[4] == "-"
    assert path.stem[7] == "-"
    assert path.stem[10] == "_"


def test_list_reports_empty(tmp_path, monkeypatch):
    """空目录返回空列表"""
    monkeypatch.setattr("scripts.history.get_history_dir", lambda: tmp_path / "history")
    assert list_reports() == []


def test_list_reports_sorted_newest_first(tmp_path, monkeypatch):
    """按时间倒序排列"""
    monkeypatch.setattr("scripts.history.get_history_dir", lambda: tmp_path / "history")

    save_report({"a": 1}, "p1", [])
    save_report({"a": 2}, "p2", [])

    reports = list_reports()
    assert len(reports) == 2


def test_load_history_returns_data(tmp_path, monkeypatch):
    """读取历史返回数据列表"""
    monkeypatch.setattr("scripts.history.get_history_dir", lambda: tmp_path / "history")
    save_report({"x": 100}, "🧱", ["i1"])
    save_report({"y": 200}, "📚", ["i2"])

    history = load_history(limit=10)
    assert len(history) == 2
    assert "by_category" in history[0] or "x" in history[0] or "y" in history[0]


def test_load_history_respects_limit(tmp_path, monkeypatch):
    """limit 限制数量"""
    monkeypatch.setattr("scripts.history.get_history_dir", lambda: tmp_path / "history")
    for i in range(5):
        save_report({"i": i}, f"p{i}", [])

    history = load_history(limit=3)
    assert len(history) == 3
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_history.py -v`
Expected: FAIL with "No module named 'scripts.history'"

- [ ] **Step 3: 实现 history 模块**

写入 `scripts/history.py`:
```python
"""
历史报告存储 - 每次生成的报告序列化到 JSON 文件
"""
import json
from datetime import datetime
from pathlib import Path
from typing import Dict, List, Optional


def get_history_dir() -> Path:
    """获取历史报告目录"""
    if hasattr(get_history_dir, "_override"):
        return get_history_dir._override  # type: ignore
    d = Path.home() / ".digital-nutrition" / "history"
    d.mkdir(parents=True, exist_ok=True)
    return d


def save_report(
    report_data: Dict,
    persona: str,
    insights: List[str],
    history_dir: Optional[Path] = None,
) -> Path:
    """
    保存报告到历史目录。
    返回写入的文件路径。
    """
    if history_dir is None:
        history_dir = get_history_dir()
    history_dir.mkdir(parents=True, exist_ok=True)

    timestamp = datetime.now()
    filename = timestamp.strftime("%Y-%m-%d_%H%M%S") + ".json"
    path = history_dir / filename

    payload = {
        "saved_at": timestamp.isoformat(),
        "persona": persona,
        "insights": insights,
        **report_data,
    }
    path.write_text(
        json.dumps(payload, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )
    return path


def list_reports(history_dir: Optional[Path] = None) -> List[Path]:
    """列出所有历史报告（按时间倒序）"""
    if history_dir is None:
        history_dir = get_history_dir()
    if not history_dir.exists():
        return []
    return sorted(history_dir.glob("*.json"), reverse=True)


def load_history(limit: int = 10, history_dir: Optional[Path] = None) -> List[Dict]:
    """读取最近 N 份历史报告"""
    paths = list_reports(history_dir)[:limit]
    return [json.loads(p.read_text(encoding="utf-8")) for p in paths]
```

- [ ] **Step 4: 运行测试**

Run: `python -m pytest tests/test_history.py -v`
Expected: PASS (7 tests)

- [ ] **Step 5: 提交**

```bash
git add scripts/history.py tests/test_history.py
git commit -m "feat: add history storage for reports"
```

---

## Task 2: 趋势对比模块

**Files:**
- Create: `digital-nutrition/scripts/trend.py`
- Create: `digital-nutrition/tests/test_trend.py`

- [ ] **Step 1: 写失败的测试**

写入 `tests/test_trend.py`:
```python
"""Tests for trend analysis."""
import pytest
from scripts.trend import (
    build_daily_aggregates,
    compare_periods,
    compute_category_deltas,
)


def test_build_daily_aggregates_basic():
    """按天聚合"""
    from datetime import datetime
    from scripts.models import Event

    events = [
        Event(datetime(2024, 6, 1, 10, 0), 1800, "browser", "code", None, "x", "u"),
        Event(datetime(2024, 6, 1, 14, 0), 3600, "browser", "learning", None, "x", "u"),
        Event(datetime(2024, 6, 2, 10, 0), 1800, "browser", "code", None, "x", "u"),
    ]
    daily = build_daily_aggregates(events)
    assert "2024-06-01" in daily
    assert daily["2024-06-01"]["code"] == 1800
    assert daily["2024-06-01"]["learning"] == 3600
    assert daily["2024-06-02"]["code"] == 1800


def test_build_daily_aggregates_empty():
    """空事件"""
    assert build_daily_aggregates([]) == {}


def test_build_daily_aggregates_sorted_by_date():
    """按日期升序"""
    from datetime import datetime
    from scripts.models import Event

    events = [
        Event(datetime(2024, 6, 3, 10, 0), 100, "browser", "code", None, "x", "u"),
        Event(datetime(2024, 6, 1, 10, 0), 200, "browser", "code", None, "x", "u"),
        Event(datetime(2024, 6, 2, 10, 0), 300, "browser", "code", None, "x", "u"),
    ]
    daily = build_daily_aggregates(events)
    dates = list(daily.keys())
    assert dates == sorted(dates)


def test_compare_periods_basic():
    """对比两个时段"""
    current = {"code": 5000, "learning": 3000, "other": 2000}
    previous = {"code": 4000, "learning": 3500, "other": 2500}
    deltas = compare_periods(current, previous)
    assert deltas["code"]["delta_pct"] == 25.0  # (5000-4000)/4000*100
    assert deltas["learning"]["delta_pct"] == pytest.approx(-14.285, abs=0.01)
    assert deltas["other"]["delta_pct"] == -20.0


def test_compare_periods_handles_zero_previous():
    """之前为 0 时不报错"""
    current = {"code": 5000, "new_cat": 1000}
    previous = {"code": 5000}
    deltas = compare_periods(current, previous)
    assert deltas["new_cat"]["delta_pct"] is None  # 无基线


def test_compute_category_deltas_keys():
    """返回所有类别"""
    current = {"code": 5000, "learning": 3000}
    previous = {"code": 4000, "learning": 3500, "work": 1000}
    deltas = compute_category_deltas(current, previous)
    assert "code" in deltas
    assert "learning" in deltas
    assert "work" in deltas  # 之前有但现在没了
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_trend.py -v`
Expected: FAIL with "No module named 'scripts.trend'"

- [ ] **Step 3: 实现 trend 模块**

写入 `scripts/trend.py`:
```python
"""
趋势分析 - 每日聚合 + 周期对比
"""
from collections import defaultdict
from datetime import datetime
from typing import Dict, List, Optional

from models import Event


def build_daily_aggregates(events: List[Event]) -> Dict[str, Dict[str, int]]:
    """
    按天聚合每个类别的总时长。
    返回 {date_str: {category: seconds}}，按日期升序。
    """
    result: Dict[str, Dict[str, int]] = defaultdict(lambda: defaultdict(int))
    for e in events:
        day_key = e.timestamp.strftime("%Y-%m-%d")
        result[day_key][e.category] += e.duration_seconds
    return {k: dict(v) for k, v in sorted(result.items())}


def compute_category_deltas(
    current: Dict[str, int],
    previous: Dict[str, int],
) -> Dict[str, Dict]:
    """
    计算每个类别的变化量与变化百分比。
    返回 {category: {current, previous, delta, delta_pct}}
    """
    all_keys = set(current.keys()) | set(previous.keys())
    result = {}
    for cat in all_keys:
        cur = current.get(cat, 0)
        prev = previous.get(cat, 0)
        delta = cur - prev
        if prev > 0:
            delta_pct = round(delta / prev * 100, 1)
        else:
            delta_pct = None  # 无基线
        result[cat] = {
            "current": cur,
            "previous": prev,
            "delta": delta,
            "delta_pct": delta_pct,
        }
    return result


def compare_periods(
    current: Dict[str, int],
    previous: Dict[str, int],
) -> Dict[str, Dict]:
    """compare_periods 是 compute_category_deltas 的语义别名"""
    return compute_category_deltas(current, previous)
```

- [ ] **Step 4: 运行测试**

Run: `python -m pytest tests/test_trend.py -v`
Expected: PASS (6 tests)

- [ ] **Step 5: 提交**

```bash
git add scripts/trend.py tests/test_trend.py
git commit -m "feat: add trend analysis with daily aggregates and deltas"
```

---

## Task 3: 趋势洞察生成器

**Files:**
- Modify: `digital-nutrition/scripts/insight.py`
- Modify: `digital-nutrition/tests/test_insight.py`

- [ ] **Step 1: 增加测试**

追加到 `tests/test_insight.py` 末尾:
```python
from scripts.insight import generate_trend_insight


def test_trend_insight_increase():
    """上升趋势"""
    deltas = {"code": {"current": 5000, "previous": 4000, "delta": 1000, "delta_pct": 25.0}}
    insight = generate_trend_insight(deltas)
    assert insight is not None
    assert "写代码" in insight
    assert "↑" in insight or "+" in insight
    assert "25" in insight


def test_trend_insight_decrease():
    """下降趋势"""
    deltas = {"entertainment": {"current": 1000, "previous": 2000, "delta": -1000, "delta_pct": -50.0}}
    insight = generate_trend_insight(deltas)
    assert insight is not None
    assert "娱乐" in insight
    assert "↓" in insight or "-" in insight


def test_trend_insight_no_previous():
    """无基线时跳过"""
    deltas = {"new": {"current": 1000, "previous": 0, "delta": 1000, "delta_pct": None}}
    assert generate_trend_insight(deltas) is None


def test_trend_insight_small_change_ignored():
    """变化小于 10% 不输出"""
    deltas = {"code": {"current": 5000, "previous": 4900, "delta": 100, "delta_pct": 2.0}}
    assert generate_trend_insight(deltas) is None


def test_trend_insight_picks_largest_change():
    """多个变化时取最大的"""
    deltas = {
        "code": {"current": 5000, "previous": 4000, "delta": 1000, "delta_pct": 25.0},
        "entertainment": {"current": 100, "previous": 1000, "delta": -900, "delta_pct": -90.0},
    }
    insight = generate_trend_insight(deltas)
    assert insight is not None
    # 应该取 entertainment 90% 下降
    assert "娱乐" in insight
```

- [ ] **Step 2: 运行测试验证失败**

Run: `python -m pytest tests/test_insight.py::test_trend_insight_increase -v`
Expected: FAIL with "cannot import name 'generate_trend_insight'"

- [ ] **Step 3: 实现 generate_trend_insight**

追加到 `scripts/insight.py` 末尾:
```python
def generate_trend_insight(deltas: Dict[str, Dict]) -> Optional[str]:
    """
    趋势型洞察：对比上周，挑选最大变化。
    deltas: 来自 trend.compute_category_deltas
    """
    # 过滤：无基线或变化过小
    candidates = []
    for cat, d in deltas.items():
        if d["delta_pct"] is None:
            continue
        if abs(d["delta_pct"]) < 10:
            continue
        candidates.append((cat, d))

    if not candidates:
        return None

    # 取绝对变化最大的
    cat, d = max(candidates, key=lambda x: abs(x[1]["delta_pct"]))
    cat_name = CATEGORY_NAMES.get(cat, cat)
    pct = d["delta_pct"]
    if pct > 0:
        arrow = "↑"
        sign = "+"
    else:
        arrow = "↓"
        sign = ""

    return f"相比上周，{cat_name}{arrow}{sign}{pct}%"


def generate_insights(
    by_category: Dict[str, int],
    by_hour: Dict[int, int],
    deltas: Optional[Dict[str, Dict]] = None,
) -> List[str]:
    """
    生成洞察列表（最多 5 条）。
    按优先级：极端型 > 趋势型 > 模式型 > 平衡型
    """
    insights = []

    # 1. 极端型（必出）
    extreme = generate_extreme_insight(by_category)
    if extreme:
        insights.append(extreme)

    # 2. 趋势型（如果有）
    if deltas:
        trend = generate_trend_insight(deltas)
        if trend:
            insights.append(trend)

    # 3. 模式型
    peak = generate_peak_hour_insight(by_hour)
    if peak:
        insights.append(peak)

    # 4. 平衡型
    balance = generate_balance_insight(by_category)
    if balance:
        insights.append(balance)

    return insights[:5]
```

- [ ] **Step 4: 修改 Optional import**

`scripts/insight.py` 顶部改为:
```python
from typing import Dict, List, Optional
```

- [ ] **Step 5: 运行测试**

Run: `python -m pytest tests/test_insight.py -v`
Expected: PASS (20 tests, 15 old + 5 new)

- [ ] **Step 6: 提交**

```bash
git add scripts/insight.py tests/test_insight.py
git commit -m "feat: add trend insight with week-over-week comparison"
```

---

## Task 4: HTML 报告模板增加趋势图

**Files:**
- Modify: `digital-nutrition/templates/report.html.j2`
- Modify: `digital-nutrition/scripts/report_generator.py`

- [ ] **Step 1: 修改模板，添加趋势图区块**

在 `templates/report.html.j2` 中找到 `<div class="chart-container">` 块（时间分布之后），插入:

```html
    {% if daily_chart_data %}
    <div class="chart-container">
      <div class="chart-title">📅 每日趋势（最近 7 天）</div>
      <div id="daily-chart" style="width: 100%; height: 320px;"></div>
    </div>
    {% endif %}
```

- [ ] **Step 2: 修改模板底部 script，添加图表初始化**

在 `<script>const chart = echarts.init(...);` 之后添加:
```javascript
    {% if daily_chart_data %}
    const dailyChart = echarts.init(document.getElementById('daily-chart'));
    dailyChart.setOption({
      tooltip: { trigger: 'axis', axisPointer: { type: 'shadow' } },
      legend: { bottom: 0, textStyle: { color: '#e2e8f0' } },
      grid: { left: 50, right: 20, top: 30, bottom: 50 },
      xAxis: { type: 'category', data: {{ daily_chart_dates | safe }}, axisLabel: { color: '#e2e8f0' } },
      yAxis: { type: 'value', axisLabel: { color: '#e2e8f0' } },
      series: {{ daily_chart_data | safe }}
    });
    {% endif %}
```

- [ ] **Step 3: 修改 report_generator.py 接受 daily_data**

在 `scripts/report_generator.py` 中修改 `render_report` 函数签名和实现:

```python
def render_report(
    report_data: Dict,
    persona: str,
    insights: List[str],
    output_dir: Path,
    daily_aggregates: Optional[Dict[str, Dict[str, int]]] = None,
) -> Path:
    # ... 现有代码 ...

    # 准备每日趋势图数据
    daily_chart_data = None
    daily_chart_dates = None
    if daily_aggregates:
        dates = sorted(daily_aggregates.keys())
        # 收集所有出现的类别
        all_cats = set()
        for d in daily_aggregates.values():
            all_cats.update(d.keys())
        cat_order = sorted(all_cats, key=lambda c: -sum(
            daily_aggregates.get(date, {}).get(c, 0) for date in dates
        ))

        # 构建 series
        cat_meta_lookup = CATEGORY_META
        series = []
        for cat in cat_order:
            meta = cat_meta_lookup.get(cat, {})
            series.append({
                "name": meta.get("name", cat),
                "type": "bar",
                "stack": "total",
                "itemStyle": {"color": meta.get("color", "#9ca3af")},
                "data": [
                    int(daily_aggregates.get(date, {}).get(cat, 0) / 60)
                    for date in dates  # 转换为分钟
                ],
            })
        daily_chart_data = json.dumps(series, ensure_ascii=False)
        daily_chart_dates = json.dumps(dates, ensure_ascii=False)

    html = template.render(
        # ... 现有参数 ...
        daily_chart_data=daily_chart_data,
        daily_chart_dates=daily_chart_dates,
    )
```

- [ ] **Step 4: 跑全部测试确保没破坏**

Run: `python -m pytest -v`
Expected: 93 + 13 = 106 tests pass

- [ ] **Step 5: 提交**

```bash
git add templates/report.html.j2 scripts/report_generator.py
git commit -m "feat: add daily trend chart to report template"
```

---

## Task 5: main.py 整合历史与趋势

**Files:**
- Modify: `digital-nutrition/scripts/main.py`
- Modify: `digital-nutrition/tests/test_main.py`

- [ ] **Step 1: 增加测试**

追加到 `tests/test_main.py`:
```python
def test_generate_report_saves_history(tmp_path):
    """生成报告时保存历史"""
    from scripts.history import get_history_dir
    import scripts.history as history_mod
    history_mod.get_history_dir._override = tmp_path / "history"

    output_dir = tmp_path / "report"

    with patch("scripts.main.collect_browser_events") as mock_browser, \
         patch("scripts.main.collect_git_events") as mock_git:
        mock_browser.return_value = []
        mock_git.return_value = [
            Event(
                timestamp=datetime(2024, 6, 1, 11, 0),
                duration_seconds=1800,
                source="git",
                category="code",
                subcategory=None,
                title="feat: x",
                url_or_path="/r",
            ),
        ]

        from datetime import datetime
        generate_report(period="weekly", output_dir=output_dir, open_browser=False)

        history = history_mod.load_history()
        assert len(history) >= 1
        assert history[0]["persona"] == "🧱 代码机器人"
```

- [ ] **Step 2: 修改 main.py**

在 `scripts/main.py` 中:
1. 顶部 import 增加 `from history import save_report, load_history` 和 `from trend import build_daily_aggregates, compare_periods`
2. 在 `generate_report` 函数中, `analyze.build_report_data` 之后:
   - 调用 `save_report(report_data, persona, insights)` 保存历史
   - 调用 `build_daily_aggregates(classified_events)` 聚合每日
   - 如果有历史, `previous = history[0]["by_category"]`, 计算 `deltas = compare_periods(by_category, previous)`
   - 重新生成 insights 包含趋势
   - 把 `daily_aggregates` 传给 `render_report`

具体代码:
```python
# 在 generate_report 函数中替换 generate_insights 调用
from history import save_report, load_history
from trend import build_daily_aggregates, compare_periods

# 之前: report_data = build_report_data(...)
# 增加: 应用分类后的事件用于每日聚合
classified_events = analyze.apply_classification(all_events, rules)
daily_aggregates = build_daily_aggregates(classified_events)

# 加载历史用于趋势对比
history = load_history(limit=1)
deltas = None
if history:
    prev_by_cat = history[0].get("by_category", {})
    deltas = compare_periods(report_data["by_category"], prev_by_cat)

# 重新生成包含趋势的洞察
insights = generate_insights(
    report_data["by_category"],
    report_data["by_hour"],
    deltas=deltas,
)

# 渲染（传入 daily_aggregates）
html_path = render_report(
    report_data, persona, insights, output_dir,
    daily_aggregates=daily_aggregates,
)

# 保存到历史（在 render 之后避免影响当前报告）
save_report(report_data, persona, insights)
```

- [ ] **Step 3: 运行测试**

Run: `python -m pytest tests/test_main.py -v`
Expected: PASS (3 tests)

- [ ] **Step 4: 跑全部测试**

Run: `python -m pytest`
Expected: 107+ tests pass

- [ ] **Step 5: 手动验证**

Run: `python -m scripts.main weekly --no-open`
Expected: 报告生成成功，包含每日趋势图区块

- [ ] **Step 6: 提交**

```bash
git add scripts/main.py tests/test_main.py
git commit -m "feat: integrate history storage and trend analysis into main"
```

---

## Task 6: 文档更新 + E2E 验证

**Files:**
- Modify: `digital-nutrition/README.md`
- Modify: `digital-nutrition/tests/test_e2e.py`

- [ ] **Step 1: 增加 E2E 测试**

追加到 `tests/test_e2e.py`:
```python
def test_history_module_exists():
    """历史模块存在"""
    from scripts import history
    assert hasattr(history, "save_report")
    assert hasattr(history, "load_history")


def test_trend_module_exists():
    """趋势模块存在"""
    from scripts import trend
    assert hasattr(trend, "build_daily_aggregates")
    assert hasattr(trend, "compare_periods")


def test_report_template_supports_daily_chart():
    """报告模板支持每日趋势"""
    template = (Path(__file__).parent.parent / "templates" / "report.html.j2").read_text(encoding="utf-8")
    assert "daily_chart_data" in template
    assert "daily-chart" in template
```

- [ ] **Step 2: 运行 E2E**

Run: `python -m pytest tests/test_e2e.py -v`
Expected: PASS (8 tests, 5 old + 3 new)

- [ ] **Step 3: 更新 README**

在 `README.md` 中"特性"一节增加:
```markdown
- 📅 **趋势对比**：自动保存历史报告，对比上周变化
- 🎨 **每日趋势图**：7 天各类别时间堆叠柱状图
```

- [ ] **Step 4: 手动端到端验证**

Run 两次以生成对比:
```bash
python -m scripts.main weekly --no-open
# 等待 1 秒后
python -m scripts.main weekly --no-open
```
Expected: 第二份报告有"相比上周"趋势洞察

- [ ] **Step 5: 跑全部测试**

Run: `python -m pytest`
Expected: ALL PASS (110+)

- [ ] **Step 6: 提交**

```bash
git add README.md tests/test_e2e.py
git commit -m "docs: document v0.2 trend features"
```

---

## 总结

完成 6 个 Task 后 v0.2 完成：

- ✅ 历史报告自动存储到 `~/.digital-nutrition/history/`
- ✅ 趋势对比洞察（"相比上周，代码时间↑25%"）
- ✅ 每日趋势图（7 天堆叠柱状图）
- ✅ 第二次运行即可看到趋势
- ✅ 约 17 个新测试

预估总耗时：~2.5 小时
